[package]
name = "pydev-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Command-line interface for the pydev installer"

[[bin]]
name = "pydev-cli"
path = "src/main.rs"

[dependencies]
pydev-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
anyhow = { workspace = true }
