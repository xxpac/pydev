[package]
name = "pydev-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared engine for the pydev installer (GUI + CLI)"

[lib]
name = "pydev_core"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
dirs = { workspace = true }
reqwest = { workspace = true }
